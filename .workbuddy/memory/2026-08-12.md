# 2026-08-12

## 复利 App 介绍落地页（HTML）
- 交付：`/Users/wusiyi/Documents/wsy/temp/CIM/compound-interest.html`（自包含单文件，亮/暗双主题，CSS 手绘仿真 mockup，无外部依赖除 Google Fonts）。
- 定位：作品集/技术展示向。板块：Hero+三大核心 / 星尘经济详解 / 界面预览 / 技术底气条（轻量补在页脚前，用户没单独选此节，可删）。
- 配色严格取自 `app/src/main/java/com/wsy/ci/core/designsystem/Color.kt` 的暖色账本 token（纸白 `#F7F3EA` + 石墨 `#211F1A` + 金铜 `#B8863B` + 电青 `#3FA9A0`），亮暗跟随主题。
- 字体：Fraunces（拉丁展示）+ Noto Serif/Sans SC，系统字体回退。
- 待用户确认：奖励点命名 —— 产品方案用「星尘」，AGENTS.md 写「CI 币」，不一致需拍板。App 显示名 manifest 是 "Compound Interest"，中文概念为「复利」。
- 坑已修：SVG 表现属性不能写 `var(--x)`，已改为 class + CSS 上色。
- 界面预览用真实 CSS 仿真（非占位框），符合用户"CSS 手绘仿真"选择。

## 命名与上传（后续轮次）
- 用户拍板：奖励点命名为 **CI币**（非产品方案里的"星尘"）。已全站替换"星尘"→"CI币"，符号 `✦` 保留作 CI币标记；英文 eyebrow "The Stardust Economy"→"The CI Economy"。
- 下载入口（Header/Hero/收尾/页脚共 4 处）全部外链到 `https://cnb.cool/110011AI/CIM`（target=_blank）。
- 用户要求**暂不 git 推送**（本地 CIM 仓库 remote 是 github.com/wusiyi110011/CIM.git，与 cnb.cool 不是同一处）。

## 新增「JSON 导入」互动板块
- 读取（只读未改）项目导入逻辑：`core/porting/CiImport.kt`（v1 格式）、`JsonText.extractJsonObject`（宽容提取 JSON、容忍 markdown 围栏与闲话）、`economy/Economy.kt` 的 Difficulty 系数。
- CI 导入 v1 格式要点：顶层 `version:1` + `domain{name, titles(恰好6或不填)}` + `quests[]{type:MAIN|SIDE, title, description, deadline(仅主线), chapters[]}` + `tasks[]{title, date(yyyy-MM-dd), start/end(HH:mm), difficulty(EASY×0.8/NORMAL×1.0/HARD×1.3/EPIC×1.6), quest(引用任务线标题), locked, note}`。校验：version=1、非空、titles 6个或不填、type 枚举、时间/难度枚举、主线最多4条；结束<开始按跨午夜。
- 网页新增 `#import` 区块：左侧字段速查+难度色阶+工程视角注记；右侧可编辑 `<textarea>` 预填真实可导入示例，点击"解析并预览"按 CiImport 逻辑解析，渲染 domain/头衔/MAIN-SIDE 任务线 chips + 任务时间线，并按 Economy 公式（加权分钟×难度系数）预估每项与合计 CI币。加载即渲染一次。
- 后续补：「AI 生成」子面板——输入一句话目标 → 本地模板生成可导入 JSON（真实 App 由 LLM `LlmTaskType.学习路线生成` 生成、断网走模板兜底）→ 复用同一解析管线渲染。含 loading 态与离线兜底说明。
