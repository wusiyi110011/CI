# 发现与决策

## 需求
- 主线卡提供“批量关联任务”入口。
- 候选列表只包含 `questId == null` 的任务。
- 支持逐项选择、全选和确认批量关联。
- 关联目标为点击入口的那条主线。
- 【日程】页增加“＋”按钮，用于新建任务。

## 研究发现
- `TaskEntity.questId` 为空即可判定“未被关联”。
- `TaskDao` 目前只有按任务线查询和单条更新，没有未关联任务查询或批量关联方法。
- 主线卡已有完整宽度，顶部操作区适合加入文字按钮；已完成/归档卡也复用同一组件。
- 工程已有 `CiFormDialog` 宽对话框规格与日期、时间格式化工具，可以直接复用。
- 工程当前没有现成的多选/全选组件，需要使用 Material 3 `Checkbox` 组合列表行。
- Room 生成代码确认未关联查询为 `questId IS NULL ORDER BY epochDay, startMinute`，批量更新方法已成功生成。
- 【日程】对应 `feature/calendar/CalendarScreen.kt`，已经复用 `TaskEditorDialog`，应直接接入现有编辑状态而不是新增表单。
- 【今日】页已有一致的右下角 `FloatingActionButton` 与新任务草稿生成逻辑，可在【日程】页复用同一视觉和交互模式。
- 【日程】现有 `saveTask` 保存后没有刷新小组件；新增入口会走该路径，必须补上刷新以符合项目约定。
- 新任务草稿应使用日程页当前选中的日期，时间默认取下一个 15 分钟刻度、时长 1 小时。

## 技术决策
| 决策 | 理由 |
|------|------|
| 只在进行中的主线卡显示入口 | 避免把新任务挂到已完成或已归档主线，与现有任务编辑器只允许关联进行中任务线的规则一致 |
| 查询按日期、起始时间排序 | 方便用户按日程顺序批量选择，并能区分同名任务 |
| 批量更新 SQL 再加 `questId IS NULL` 条件 | 即使弹窗打开后任务被其他操作关联，也不会覆盖既有关联 |
| 批量关联后刷新小组件 | 项目约定要求所有任务数据改动后刷新小组件 |
| 日程页使用与今日页一致的右下角 FAB | 保持导航页面交互一致，且用户明确要求“＋”按钮 |
| 新任务日期取日程当前选中日 | 用户在日、周、月视图浏览哪一天，就应默认添加到那一天 |

## 遇到的问题
| 问题 | 解决方案 |
|------|---------|
| 取消后立即重开时自动化未提取到文本 | 不重复快速点击；以已成功获取的首次弹窗 UI 树和全选后 UI 树作为实机证据 |

## 视觉/实机发现
- 平板【任务】页的两条进行中主线均显示“批量关联任务”入口。
- 点击第一条主线入口后，弹窗显示 154 个未关联任务，按日期和时间升序排列。
- 点击“全选（154 个）”后，可见候选复选框全部变为选中，底部出现“关联 154 个”确认按钮。
- 实机验证未点击确认按钮，没有改动用户现有任务关联。
- 全选后的视觉布局正常，长任务列表在对话框内滚动，确认与取消按钮保持固定可见。
- 【日程】日视图右下角已显示“＋”悬浮按钮，位置与【今日】页一致，未遮挡主要时间线内容。
- 点击日程页“＋”后成功打开“新建任务”弹窗；实机默认日期为当前选中日 `2026-08-07`，时间为下一个 15 分钟刻度 `20:15–21:15`。
- 弹窗完整显示领域、主线/支线、锁定、备注等既有字段；验证未保存测试任务。
- 收紧代码差异后重新构建并覆盖安装，最终安装版本仍能识别【日程】和“＋”入口。
